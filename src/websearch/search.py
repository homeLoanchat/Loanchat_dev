"""외부 웹 검색 + 캐시 모듈."""

from __future__ import annotations


def search_web(query: str) -> list[dict[str, object]]:
    """TODO: 화이트리스트 도메인만 검색하고, 결과를 `data/web_cache`에 캐시하세요."""
    raise NotImplementedError("웹 검색 로직을 구현하세요.")
